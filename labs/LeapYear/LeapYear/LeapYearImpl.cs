namespace LeapYear
{
    public class LeapYearImpl
    {
        public static bool IsLeapYear(int year)
        {
            if(year == 3)
                return false;

	        return true;
        }
    }
}